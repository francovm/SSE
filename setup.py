from setuptools import find_packages, setup

setup(
    name='sse',
    packages=find_packages(),
    version='0.1.0',
    description='A data science project to automated slow slip event detection and clustering using GNSS data. This project could extend to the use of other data sets.',
    author='Franco Vega',
    license='',
)
